local wezterm = require("wezterm")


return {
    -- colors = theme.colors(),
    font = wezterm.font("MapleMono NF"),
    color_scheme = "N0Tch2K",
    enable_tab_bar = false,
    font_size = 18.0,
}
