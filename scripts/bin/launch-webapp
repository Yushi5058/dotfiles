#!/bin/bash
# Usage: launch-webapp discord https://discord.com/app

NAME=$1
URL=$2
DIR="$HOME/.cache/qute-$NAME"

mkdir -p "$DIR"
qutebrowser --basedir "$DIR" \
            --target window \
            -s content.user_stylesheets "['~/.config/qutebrowser/minimal.css']" \
            "$URL"